[gd_scene load_steps=43 format=3 uid="uid://c1khhbxfiouub"]

[ext_resource type="Script" uid="uid://c5bjtkua4th4m" path="res://Scripts/air_explosion.gd" id="1_voq2d"]

[sub_resource type="Shader" id="Shader_h7wn8"]
code = "shader_type canvas_item;

// Uniforms for customization
uniform sampler2D noise_texture : repeat_enable;

// Two color gradients representing the two color variants over the particle's lifetime (0.0 to 1.0)
uniform sampler2D gradient_1 : source_color, hint_default_white;
uniform sampler2D gradient_2 : source_color, hint_default_white;

uniform float distortion_speed : hint_range(0.0, 5.0) = 0.2;
uniform float density : hint_range(0.0, 1.0) = 1.0;
uniform float spread : hint_range(0.0, 1.0) = 0.4;    // Controls how far the smoke reaches
uniform float erosion : hint_range(0.0, 1.0) = 0.3;   // Controls how jagged/organic the outer edges are
uniform float pixel_size : hint_range(1.0, 64.0) = 16.0; // Controls the pixel art grid resolution
uniform float color_threshold : hint_range(0.0, 1.0) = 0.5; // Split point between the two colors

// Varying variable to pass lifetime from vertex processor to fragment processor
varying float life_percent;

void vertex() {
    // INSTANCE_CUSTOM.y automatically holds the GPUParticle lifetime ratio (0.0 to 1.0)
    life_percent = INSTANCE_CUSTOM.y;
}

void fragment() {
    // Pixelate UV coordinates to create a chunky pixel art look
    vec2 p_uv = floor(UV * pixel_size) / pixel_size;
    
    // Pre-calculate time-based offsets
    float time_offset_1 = TIME * distortion_speed * 0.1;
    float time_offset_2 = TIME * distortion_speed * 0.2;
    
    // Sample noise with distortion for the internal texture using pixelated UV
    vec4 noise = texture(noise_texture, p_uv + time_offset_1);
    
    // Create distorted UV and sample final noise
    vec2 distorted_uv = p_uv + noise.rg * 0.1 - 0.05 + time_offset_2;
    float final_noise = texture(noise_texture, distorted_uv).r;
    
    // Calculate distance from the center (0.5, 0.5) using pixelated UV
    float dist = distance(p_uv, vec2(0.5));
    
    // Sample the noise texture to erode the edges organically
    float edge_noise = texture(noise_texture, (p_uv * 0.7) + (time_offset_1 * 0.5)).r;
    
    // Modulate the distance check with the noise texture
    float organic_mask = dist + (edge_noise - 0.5) * erosion;
    
    // Create a sharp step cutoff for pixel art instead of smoothstep
    float radial_mask = step(organic_mask, spread);
    
    // Combine noise, density, and the organic erosion mask
    float smoke_alpha = final_noise * density * radial_mask;
    
    // Sample the two independent color gradients based on particle age (life_percent)
    vec4 active_color_1 = texture(gradient_1, vec2(life_percent, 0.0));
    vec4 active_color_2 = texture(gradient_2, vec2(life_percent, 0.0));
    
    // Posterize to two distinct shades or transparent
    vec4 final_color = vec4(0.0);
    if (smoke_alpha > 0.01) {
        if (final_noise > color_threshold) {
            final_color = active_color_2;
        } else {
            final_color = active_color_1;
        }
        // Multiply by the radial mask or incorporate color alpha
        final_color.a *= radial_mask;
    }
    
    // Output color
    COLOR = final_color;
}"

[sub_resource type="Gradient" id="Gradient_l7edp"]
colors = PackedColorArray(0.09803922, 0.09803922, 0.09803922, 1, 0.2, 0.2, 0.2, 0)

[sub_resource type="GradientTexture1D" id="GradientTexture1D_heume"]
gradient = SubResource("Gradient_l7edp")

[sub_resource type="Gradient" id="Gradient_51dre"]
offsets = PackedFloat32Array(0, 0.989899)
colors = PackedColorArray(0.10980392, 0.10980392, 0.10980392, 0.64705884, 0.4, 0.4, 0.4, 0)

[sub_resource type="GradientTexture1D" id="GradientTexture1D_j0a41"]
gradient = SubResource("Gradient_51dre")

[sub_resource type="FastNoiseLite" id="FastNoiseLite_l7edp"]

[sub_resource type="NoiseTexture2D" id="NoiseTexture2D_heume"]
noise = SubResource("FastNoiseLite_l7edp")

[sub_resource type="ShaderMaterial" id="ShaderMaterial_j0a41"]
resource_local_to_scene = true
shader = SubResource("Shader_h7wn8")
shader_parameter/noise_texture = SubResource("NoiseTexture2D_heume")
shader_parameter/gradient_1 = SubResource("GradientTexture1D_heume")
shader_parameter/gradient_2 = SubResource("GradientTexture1D_j0a41")
shader_parameter/distortion_speed = 0.2
shader_parameter/density = 1.0
shader_parameter/spread = 0.3600000171
shader_parameter/erosion = 0.3
shader_parameter/pixel_size = 32.0000014725
shader_parameter/color_threshold = 0.5

[sub_resource type="Curve" id="Curve_gm6ti"]
_data = [Vector2(0, 0.7522422), 0.0, 0.0, 0, 0, Vector2(1, 1), 0.0, 0.0, 0, 0]
point_count = 2

[sub_resource type="CurveTexture" id="CurveTexture_st2g1"]
curve = SubResource("Curve_gm6ti")

[sub_resource type="ParticleProcessMaterial" id="ParticleProcessMaterial_agenq"]
lifetime_randomness = 0.16
particle_flag_disable_z = true
angle_min = -180.0
angle_max = 180.0
spread = 102.1
angular_velocity_max = 46.45
gravity = Vector3(0, 0, 0)
radial_accel_max = 10.0
scale_min = 10.0
scale_max = 30.0
scale_curve = SubResource("CurveTexture_st2g1")

[sub_resource type="Shader" id="Shader_ehuqm"]
code = "shader_type canvas_item;

// Uniforms for customization
uniform sampler2D noise_texture : repeat_enable;

// Two color gradients representing the two color variants over the particle's lifetime (0.0 to 1.0)
uniform sampler2D gradient_1 : source_color, hint_default_white;
uniform sampler2D gradient_2 : source_color, hint_default_white;

uniform float distortion_speed : hint_range(0.0, 5.0) = 0.2;
uniform float density : hint_range(0.0, 1.0) = 1.0;
uniform float spread : hint_range(0.0, 1.0) = 0.4;    // Controls how far the smoke reaches
uniform float erosion : hint_range(0.0, 1.0) = 0.3;   // Controls how jagged/organic the outer edges are
uniform float pixel_size : hint_range(1.0, 64.0) = 16.0; // Controls the pixel art grid resolution
uniform float color_threshold : hint_range(0.0, 1.0) = 0.5; // Split point between the two colors

// Varying variable to pass lifetime from vertex processor to fragment processor
varying float life_percent;

void vertex() {
    // INSTANCE_CUSTOM.y automatically holds the GPUParticle lifetime ratio (0.0 to 1.0)
    life_percent = INSTANCE_CUSTOM.y;
}

void fragment() {
    // Pixelate UV coordinates to create a chunky pixel art look
    vec2 p_uv = floor(UV * pixel_size) / pixel_size;
    
    // Pre-calculate time-based offsets
    float time_offset_1 = TIME * distortion_speed * 0.1;
    float time_offset_2 = TIME * distortion_speed * 0.2;
    
    // Sample noise with distortion for the internal texture using pixelated UV
    vec4 noise = texture(noise_texture, p_uv + time_offset_1);
    
    // Create distorted UV and sample final noise
    vec2 distorted_uv = p_uv + noise.rg * 0.1 - 0.05 + time_offset_2;
    float final_noise = texture(noise_texture, distorted_uv).r;
    
    // Calculate distance from the center (0.5, 0.5) using pixelated UV
    float dist = distance(p_uv, vec2(0.5));
    
    // Sample the noise texture to erode the edges organically
    float edge_noise = texture(noise_texture, (p_uv * 0.7) + (time_offset_1 * 0.5)).r;
    
    // Modulate the distance check with the noise texture
    float organic_mask = dist + (edge_noise - 0.5) * erosion;
    
    // Create a sharp step cutoff for pixel art instead of smoothstep
    float radial_mask = step(organic_mask, spread);
    
    // Combine noise, density, and the organic erosion mask
    float smoke_alpha = final_noise * density * radial_mask;
    
    // Sample the two independent color gradients based on particle age (life_percent)
    vec4 active_color_1 = texture(gradient_1, vec2(life_percent, 0.0));
    vec4 active_color_2 = texture(gradient_2, vec2(life_percent, 0.0));
    
    // Posterize to two distinct shades or transparent
    vec4 final_color = vec4(0.0);
    if (smoke_alpha > 0.01) {
        if (final_noise > color_threshold) {
            final_color = active_color_2;
        } else {
            final_color = active_color_1;
        }
        // Multiply by the radial mask or incorporate color alpha
        final_color.a *= radial_mask;
    }
    
    // Output color
    COLOR = final_color;
}"

[sub_resource type="Gradient" id="Gradient_55sqh"]
colors = PackedColorArray(1, 1, 0, 1, 0.30588236, 0.08235294, 0.03137255, 0.76862746)

[sub_resource type="GradientTexture1D" id="GradientTexture1D_pc06w"]
gradient = SubResource("Gradient_55sqh")

[sub_resource type="Gradient" id="Gradient_7mq4j"]
colors = PackedColorArray(1, 0.36078432, 0, 0.64705884, 0.101960786, 0.015686275, 0, 0.42352942)

[sub_resource type="GradientTexture1D" id="GradientTexture1D_ewi0j"]
gradient = SubResource("Gradient_7mq4j")

[sub_resource type="ShaderMaterial" id="ShaderMaterial_7f0us"]
resource_local_to_scene = true
shader = SubResource("Shader_ehuqm")
shader_parameter/noise_texture = SubResource("NoiseTexture2D_heume")
shader_parameter/gradient_1 = SubResource("GradientTexture1D_pc06w")
shader_parameter/gradient_2 = SubResource("GradientTexture1D_ewi0j")
shader_parameter/distortion_speed = 0.2
shader_parameter/density = 1.0
shader_parameter/spread = 0.3600000171
shader_parameter/erosion = 0.3
shader_parameter/pixel_size = 32.0000014725
shader_parameter/color_threshold = 0.5

[sub_resource type="CurveTexture" id="CurveTexture_fmle0"]
curve = SubResource("Curve_gm6ti")

[sub_resource type="ParticleProcessMaterial" id="ParticleProcessMaterial_b0apl"]
lifetime_randomness = 0.16
particle_flag_disable_z = true
angle_min = -180.0
angle_max = 180.0
spread = 102.1
angular_velocity_max = 46.45
gravity = Vector3(0, 0, 0)
radial_accel_max = 10.0
scale_min = 10.0
scale_max = 30.0
scale_curve = SubResource("CurveTexture_fmle0")

[sub_resource type="Gradient" id="Gradient_aqjd0"]
offsets = PackedFloat32Array(0, 0.8, 1)
colors = PackedColorArray(0.9882353, 1, 0.6862745, 1, 1, 1, 0, 1, 1, 0.5529412, 0, 1)

[sub_resource type="GradientTexture1D" id="GradientTexture1D_td6ks"]
gradient = SubResource("Gradient_aqjd0")

[sub_resource type="ParticleProcessMaterial" id="ParticleProcessMaterial_l7edp"]
resource_local_to_scene = true
particle_flag_disable_z = true
spread = 180.0
initial_velocity_max = 100.0
gravity = Vector3(0, 0, 0)
tangential_accel_min = -100.0
tangential_accel_max = 99.99999
color_ramp = SubResource("GradientTexture1D_td6ks")

[sub_resource type="Shader" id="Shader_syf3v"]
code = "shader_type canvas_item;

// Uniforms for customization
uniform sampler2D noise_texture : repeat_enable;

// Two color gradients representing the two color variants over the particle's lifetime (0.0 to 1.0)
uniform sampler2D gradient_1 : source_color, hint_default_white;
uniform sampler2D gradient_2 : source_color, hint_default_white;

uniform float distortion_speed : hint_range(0.0, 5.0) = 0.2;
uniform float density : hint_range(0.0, 1.0) = 1.0;
uniform float spread : hint_range(0.0, 1.0) = 0.4;    // Controls how far the smoke reaches
uniform float erosion : hint_range(0.0, 1.0) = 0.3;   // Controls how jagged/organic the outer edges are
uniform float pixel_size : hint_range(1.0, 64.0) = 16.0; // Controls the pixel art grid resolution
uniform float color_threshold : hint_range(0.0, 1.0) = 0.5; // Split point between the two colors

// Varying variable to pass lifetime from vertex processor to fragment processor
varying float life_percent;

void vertex() {
    // INSTANCE_CUSTOM.y automatically holds the GPUParticle lifetime ratio (0.0 to 1.0)
    life_percent = INSTANCE_CUSTOM.y;
}

void fragment() {
    // Pixelate UV coordinates to create a chunky pixel art look
    vec2 p_uv = floor(UV * pixel_size) / pixel_size;
    
    // Pre-calculate time-based offsets
    float time_offset_1 = TIME * distortion_speed * 0.1;
    float time_offset_2 = TIME * distortion_speed * 0.2;
    
    // Sample noise with distortion for the internal texture using pixelated UV
    vec4 noise = texture(noise_texture, p_uv + time_offset_1);
    
    // Create distorted UV and sample final noise
    vec2 distorted_uv = p_uv + noise.rg * 0.1 - 0.05 + time_offset_2;
    float final_noise = texture(noise_texture, distorted_uv).r;
    
    // Calculate distance from the center (0.5, 0.5) using pixelated UV
    float dist = distance(p_uv, vec2(0.5));
    
    // Sample the noise texture to erode the edges organically
    float edge_noise = texture(noise_texture, (p_uv * 0.7) + (time_offset_1 * 0.5)).r;
    
    // Modulate the distance check with the noise texture
    float organic_mask = dist + (edge_noise - 0.5) * erosion;
    
    // Create a sharp step cutoff for pixel art instead of smoothstep
    float radial_mask = step(organic_mask, spread);
    
    // Combine noise, density, and the organic erosion mask
    float smoke_alpha = final_noise * density * radial_mask;
    
    // Sample the two independent color gradients based on particle age (life_percent)
    vec4 active_color_1 = texture(gradient_1, vec2(life_percent, 0.0));
    vec4 active_color_2 = texture(gradient_2, vec2(life_percent, 0.0));
    
    // Posterize to two distinct shades or transparent
    vec4 final_color = vec4(0.0);
    if (smoke_alpha > 0.01) {
        if (final_noise > color_threshold) {
            final_color = active_color_2;
        } else {
            final_color = active_color_1;
        }
        // Multiply by the radial mask or incorporate color alpha
        final_color.a *= radial_mask;
    }
    
    // Output color
    COLOR = final_color;
}"

[sub_resource type="Gradient" id="Gradient_02lrl"]
offsets = PackedFloat32Array(0, 0.22895622, 0.8, 1)
colors = PackedColorArray(0.9893481, 1, 0.6877252, 1, 1, 1, 0, 1, 0.30588236, 0.08235294, 0.03137255, 0.6784314, 0.044808514, 0.004440951, 0.0014412964, 0)

[sub_resource type="GradientTexture1D" id="GradientTexture1D_havbn"]
gradient = SubResource("Gradient_02lrl")

[sub_resource type="Gradient" id="Gradient_oves0"]
colors = PackedColorArray(1, 0.36078432, 0, 0.64705884, 0.101960786, 0.015686275, 0, 0)

[sub_resource type="GradientTexture1D" id="GradientTexture1D_lu3uo"]
gradient = SubResource("Gradient_oves0")

[sub_resource type="FastNoiseLite" id="FastNoiseLite_fgfee"]

[sub_resource type="NoiseTexture2D" id="NoiseTexture2D_v1eya"]
noise = SubResource("FastNoiseLite_fgfee")

[sub_resource type="ShaderMaterial" id="ShaderMaterial_xj4pt"]
resource_local_to_scene = true
shader = SubResource("Shader_syf3v")
shader_parameter/noise_texture = SubResource("NoiseTexture2D_v1eya")
shader_parameter/gradient_1 = SubResource("GradientTexture1D_havbn")
shader_parameter/gradient_2 = SubResource("GradientTexture1D_lu3uo")
shader_parameter/distortion_speed = 0.2
shader_parameter/density = 1.0
shader_parameter/spread = 0.3600000171
shader_parameter/erosion = 0.3
shader_parameter/pixel_size = 32.0000014725
shader_parameter/color_threshold = 0.5

[sub_resource type="Curve" id="Curve_h7wn8"]
_limits = [-10.0, 10.0, 0.0, 1.0]
_data = [Vector2(0, 0), 0.0, 0.0, 0, 0, Vector2(0.5, 0), 0.0, 0.0, 0, 0, Vector2(1, 10), 0.0, 0.0, 0, 0]
point_count = 3

[sub_resource type="CurveTexture" id="CurveTexture_gm6ti"]
curve = SubResource("Curve_h7wn8")

[sub_resource type="CurveTexture" id="CurveTexture_bg3wq"]
curve = SubResource("Curve_gm6ti")

[sub_resource type="ParticleProcessMaterial" id="ParticleProcessMaterial_7f0us"]
particle_flag_disable_z = true
angle_min = -180.0
angle_max = 180.0
spread = 180.0
initial_velocity_min = 20.0
initial_velocity_max = 20.0
angular_velocity_min = 10.0
angular_velocity_max = 10.0
angular_velocity_curve = SubResource("CurveTexture_gm6ti")
gravity = Vector3(0, 0, 0)
linear_accel_min = -50.0
linear_accel_max = -50.0
radial_accel_max = 33.66
scale_min = 10.0
scale_max = 30.0
scale_curve = SubResource("CurveTexture_bg3wq")

[sub_resource type="Shader" id="Shader_gm6ti"]
code = "shader_type canvas_item;

// Set this to your game's canvas target size (e.g., 64.0, 128.0, 256.0)
uniform float base_resolution : hint_range(16.0, 512.0, 1.0) = 128.0;

// Controls the pixel art grid size directly from the inspector
uniform float pixel_size : hint_range(1.0, 32.0, 1.0) = 4.0;

// Uniforms for the two solid water colors (no background color, fully opaque puddle)
uniform sampler2D noise_texture : hint_default_black, repeat_enable;
uniform vec4 outer_water_color : source_color = vec4(0.1, 0.3, 0.5, 1.0); // Outer shade
uniform vec4 inner_water_color : source_color = vec4(0.2, 0.5, 0.8, 1.0); // Inner shade

// Controls to look and hollow center of the puddle
uniform float irregularity : hint_range(0.0, 1.0) = 0.35; // How ragged the outer edge is
uniform float base_inner_radius : hint_range(0.0, 0.7) = 0.1; // Starting hole size

// Passes particle lifetime progress from vertex to fragment function
varying float particle_lifetime_progress;

void vertex() {
	// INSTANCE_CUSTOM.y tracks particle lifetime progress from 0.0 (spawn) to 1.0 (death)
	particle_lifetime_progress = INSTANCE_CUSTOM.y;
}

void fragment() {
	// Use a fixed resolution grid instead of textureSize to guarantee visibility
	float grid_scale = base_resolution / pixel_size;
	vec2 pixel_uv = floor(UV * grid_scale) / grid_scale;
	
	// Center the pixelated UV coordinates (-0.5 to 0.5)
	vec2 centered_uv = pixel_uv - vec2(0.5);
	float dist = length(centered_uv) * 2.0; // Distance from center (0 to 1)
	
	// Sample noise using pixelated angles and distance
	float angle = atan(centered_uv.y, centered_uv.x);
	vec2 noise_uv = vec2(angle / 6.28318 + 0.5, dist);
	float noise_val = texture(noise_texture, noise_uv).r;
	
	// Displace the effective outer radius dynamically using the noise
	float outer_radius = 1.0 - (noise_val * irregularity);
	
	// 1. Outer edge mask: completely sharp pixel art threshold
	float outer_mask = step(dist, outer_radius);
	
	// 2. Expand inner radius over particle lifetime
	float dynamic_inner_radius = mix(base_inner_radius, 1.2, particle_lifetime_progress);
	float inner_masked_radius = dynamic_inner_radius + (noise_val * 0.1); 
	
	// Inner edge mask: sharp center cutout
	float inner_mask = step(inner_masked_radius, dist);
	
	// Combine masks to isolate the ring body
	float puddle_mask = outer_mask * inner_mask;
	
	// Determine the transition point between inner and outer shades inside the ring
	float mid_radius = mix(inner_masked_radius, outer_radius, 0.5);
	float shade_transition = step(mid_radius, dist);
	
	// Blend between inner and outer water colors based on distance from center
	vec4 selected_water_color = mix(inner_water_color, outer_water_color, shade_transition);
	
	// Output color using the puddle mask for transparency outside the shape
	COLOR = selected_water_color;
	COLOR.a *= puddle_mask;
}"

[sub_resource type="FastNoiseLite" id="FastNoiseLite_bg3wq"]

[sub_resource type="NoiseTexture2D" id="NoiseTexture2D_7f0us"]
noise = SubResource("FastNoiseLite_bg3wq")

[sub_resource type="ShaderMaterial" id="ShaderMaterial_td6ks"]
resource_local_to_scene = true
shader = SubResource("Shader_gm6ti")
shader_parameter/base_resolution = 480.0
shader_parameter/pixel_size = 4.0
shader_parameter/noise_texture = SubResource("NoiseTexture2D_7f0us")
shader_parameter/outer_water_color = Color(0.5176471, 0.9098039, 1, 1)
shader_parameter/inner_water_color = Color(0.33333334, 0.8784314, 1, 1)
shader_parameter/irregularity = 0.35
shader_parameter/base_inner_radius = 0.1

[sub_resource type="Curve" id="Curve_bg3wq"]
_limits = [20.0, 75.0, 0.0, 1.0]
_data = [Vector2(0, 20), 0.0, 0.0, 0, 0, Vector2(1, 75), 0.0, 0.0, 0, 0]
point_count = 2

[sub_resource type="CurveTexture" id="CurveTexture_aqjd0"]
curve = SubResource("Curve_bg3wq")

[sub_resource type="ParticleProcessMaterial" id="ParticleProcessMaterial_td6ks"]
resource_local_to_scene = true
particle_flag_disable_z = true
angle_max = 180.0
gravity = Vector3(0, 0, 0)
scale_curve = SubResource("CurveTexture_aqjd0")

[node name="Explosion" type="Node2D"]
script = ExtResource("1_voq2d")
exit_fire_time = 1.0
exit_smoke_time = 0.5

[node name="SmokeParticle" type="GPUParticles2D" parent="."]
material = SubResource("ShaderMaterial_j0a41")
amount = 32
fixed_fps = 0
draw_order = 0
process_material = SubResource("ParticleProcessMaterial_agenq")

[node name="FireParticle" type="GPUParticles2D" parent="."]
material = SubResource("ShaderMaterial_7f0us")
emitting = false
amount = 16
lifetime = 0.25
fixed_fps = 0
draw_order = 0
process_material = SubResource("ParticleProcessMaterial_b0apl")

[node name="AmberParticle" type="GPUParticles2D" parent="."]
emitting = false
amount = 128
lifetime = 2.0
one_shot = true
preprocess = 0.75
speed_scale = 2.0
explosiveness = 0.3
fixed_fps = 0
draw_order = 0
process_material = SubResource("ParticleProcessMaterial_l7edp")

[node name="ExplosionParticle" type="GPUParticles2D" parent="."]
material = SubResource("ShaderMaterial_xj4pt")
emitting = false
amount = 256
lifetime = 3.0
one_shot = true
speed_scale = 12.0
fixed_fps = 0
process_material = SubResource("ParticleProcessMaterial_7f0us")

[node name="MiniSplashParticle" type="GPUParticles2D" parent="."]
material = SubResource("ShaderMaterial_td6ks")
emitting = false
amount = 4
one_shot = true
preprocess = 0.25
speed_scale = 4.0
process_material = SubResource("ParticleProcessMaterial_td6ks")
